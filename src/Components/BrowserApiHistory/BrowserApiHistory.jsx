import React from "react";

function BrowserApiHistory({ className }) {
  return (
    <div className={[className, "browser__ap__historyi"].join(" ")}>
      BrowserApiHistory
    </div>
  );
}

export default BrowserApiHistory;
